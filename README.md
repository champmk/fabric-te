# fabric-te

Joint placement and path admission for a simulated GPU fabric.

**Start here if you are a model or a new session:** `AGENTS.md`, then `STATUS.md`, then `docs/DESIGN.md`.

Human-readable spec: `docs/DESIGN.html`.
